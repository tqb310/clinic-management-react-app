import React from 'react';
import {Box} from '@mui/material';
import {styled} from '@mui/material/styles';

export const FlexBox = styled(Box)`
    display: flex;
    flexDirection: row;
`

export const InFlexBox = styled(Box)`
    display: inline-flex;
    
`

